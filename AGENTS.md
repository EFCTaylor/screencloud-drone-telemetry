# Repository Guidance

- This repository currently contains only `README.md`; no application source, package manifest, lockfile, test configuration, Docker configuration, CI workflow, or runnable scripts are present.
- Treat the README as an unfinished design template: bracketed sections and commands such as `npm run dev`, `npm test`, and `docker compose up -d` are not verified executable workflows.
- Before implementing the telemetry pipeline, establish and document the chosen runtime, dependency manifest, local infrastructure, and test commands; update this file with those verified commands afterward.
